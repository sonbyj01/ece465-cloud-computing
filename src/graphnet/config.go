package graphnet

import "io"

// ReadConfig reads a network configuration from file and returns the nodes
// for use by the master node
func ReadConfig(r *io.Reader) []Node {
	// TODO
	return make([]Node, 0)
}